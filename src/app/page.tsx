/* Home page — sticky scroll pages with persistent background video. */
import ScrollExperience from "@/components/scroll-experience";

/**
 * Landing page: 5 sticky scroll pages (Hero → Value Cards →
 * Agency → Serotonin → Contact Form).
 */
export default function Home() {
  return (
    <main>
      <ScrollExperience />
    </main>
  );
}
